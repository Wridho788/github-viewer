import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["source.unsplash.com"], // Tambahkan domain Unsplash
  },
  // Tambahkan opsi konfigurasi lainnya di sini jika diperlukan
};

export default nextConfig;
